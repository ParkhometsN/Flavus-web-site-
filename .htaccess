# Разрешаем доступ ко всем
Require all granted

# Альтернативный вариант (если старый сервер)
# Order allow,deny
# Allow from all

# Включаем PHP-обработку (если нужно)
<IfModule mod_php7.c>
    php_flag display_errors on
</IfModule>
